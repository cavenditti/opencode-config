# Dependencies
node_modules/

# Lockfiles — bun.lock is the primary lockfile; ignore npm's
package-lock.json

# Local overrides & secrets (never publish these)
opencode.local.json
opencode.local.jsonc
.env
.env.local
.env.*.local

# Logs
*.log

# OS / editor noise
.DS_Store
.idea/
.vscode/