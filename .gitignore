# Dependencies
node_modules/

# Lockfiles — pnpm-lock.yaml is the primary lockfile; ignore others'
package-lock.json
bun.lock
bun.lockb

# Local overrides & secrets (never publish these)
opencode.local.json
opencode.local.jsonc
.env
.env.local
.env.*.local

# Logs
*.log

# OS / editor noise
.DS_Store
.idea/
.vscode/